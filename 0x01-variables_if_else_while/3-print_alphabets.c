#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */
int main(void)
{
	char c;
	char m;

	c = 'a';
	m ='A';
	while
           (c <= 'z') {
			putchar(c);
			c++;
		}
	while
		(c <= 'z') {
			putchar(m);
			m++;
		}
	putchar('\n');
	return (0);
}
                
