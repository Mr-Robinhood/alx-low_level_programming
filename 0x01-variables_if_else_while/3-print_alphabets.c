#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */
int main(void)
{
        char c;

        c = 'a';

	m ='A';
        while
           (c <= 'z' && m <= 'Z') {
                        putchar(c);
			putchar(m);
                        c++;
			m++;
                }
        putchar('\n');
        return (0);
}
~                
