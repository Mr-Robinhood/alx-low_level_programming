#include "main.h"
#include <stdio.h>

/**
 * mul - A function that multiplies 2 integers.
 * @a: input for first integer.
 * @b: input for the second integer.
 * Return: a * b .
 */
int mul(int a, int b)
{
	return (a * b);
}
